import 'package:flutter/material.dart';

class AlertCleaner extends StatefulWidget {
  const AlertCleaner({Key? key}) : super(key: key);

  @override
  State<AlertCleaner> createState() => _AlertCleanerState();
}

class _AlertCleanerState extends State<AlertCleaner> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
